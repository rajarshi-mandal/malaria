# Python 3.13. Pinned to the versions used for the reported results.
# CPU only, no GPU/CUDA required. All heavy jobs run single-threaded.
# (set OMP_NUM_THREADS=1 MKL_NUM_THREADS=1) because numba provides the speedup.
numpy==2.4.*
scipy==1.16.*
pandas==3.0.*
scikit-learn==1.7.*
numba==0.65.*
torch==2.8.*          # CPU build
gymnasium==1.3.*
stable-baselines3==2.9.*
SALib                  # Sobol / LHS-PRCC sensitivity
shap
matplotlib
